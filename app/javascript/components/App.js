import React from "react";
import { Route, Switch } from "react-router-dom";

const App = () => {
  return (
    <Switch>
      <Route exact path="/" component={} />
      <Route exact path="/" component={} />
    </Switch>
  );
};

export default App;
